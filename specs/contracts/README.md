# 契约规格

本目录保存任务级契约。正式聚合契约位于 `/contracts`。

契约类型：

- OpenAPI；
- Async/Event Schema；
- Plugin Manifest；
- Deployment Profile；
- Configuration Schema；
- AI Provider Schema；
- Worker Capability Schema。

任何行为变化必须先更新契约并通过兼容检查。
