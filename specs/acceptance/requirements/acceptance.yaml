{$schema: acceptance.schema.yaml, schemaVersion: 1, contractId: ACCEPTANCE-TRACE-V1, status: frozen, authority: {requirements: specs/requirements/v1/requirements.yaml, product: specs/requirements/product-requirements.md}, acceptances: [{id: ACCEPT-SOURCE-SYNC, title: 来源同步与增量一致性, requirementIds: [REQ-V1-0001], scenarios: [{requirementId: REQ-V1-0001, type: success, given: 合法数据源配置且存在可读取对象, when: 执行首次或增量同步, then: 来源对象以稳定来源标识进入目录且不要求全量复制内容}, {requirementId: REQ-V1-0001, type: failure, given: 连接器返回 429/5xx 或网络中断, when: 同步遇到失败, then: 失败被分类和重试且不产生伪成功或重复目录对象}, {requirementId: REQ-V1-0001, type: recovery, given: 同步中途失败且存在检查点, when: 故障恢复后重试, then: 从安全检查点继续或幂等重跑且不静默遗漏对象}, {requirementId: REQ-V1-0001, type: data_integrity, given: 某个 SourceObject 被删除或来源暂时不可用, when: 执行来源同步和资产重算, then: 仅标记该来源关系不可用或删除，不自动删除逻辑 Asset；其他 SourceObject、缓存、Rendition 和正式 Replica 继续保留}]}, {id: ACCEPT-CONNECTOR-CONTRACT, title: 统一 Connector 能力合同, requirementIds: [REQ-V1-0001], scenarios: [{requirementId: REQ-V1-0001, type: production_gate, given: 一个 V1 数据源被声明为可发布, when: 执行契约、失败路径、权限和 Evidence 检查, then: 只有统一能力矩阵、凭据边界和真实 Evidence 完整时才允许进入生产门禁}, {requirementId: REQ-V1-0001, type: failure, given: 接入依赖未批准私有接口或不可维护抓取方式, when: 执行生产接入评审, then: 接入被阻断且 POC 脚本不得冒充生产实现}]}, {id: 验收V1-0001, title: 统一资产归一与版本完整性, requirementIds: [REQ-V1-0002], scenarios: [{requirementId: REQ-V1-0002, type: success, given: 同一内容出现移动、改名、多来源或不同编码实体, when: 执行资产归一和版本判定, then: Asset 与 SourceObject/AssetVersion/Rendition/Replica 关系保持可追溯}, {requirementId: REQ-V1-0002, type: failure, given: 哈希、来源版本或规则无法证明两个实体相同, when: 尝试自动合并, then: 不得不可逆合并并保留独立实体或进入审查}, {requirementId: REQ-V1-0002, type: data_integrity, given: 同一内容来自两个具有不同 ACL、标签、路径或 Retention 策略的来源, when: 执行重复检测或 Asset 合并, then: 可共享逻辑内容身份但必须保留两个独立 SourceObject 及各自来源级策略，哈希相等不得静默覆盖策略}, {requirementId: REQ-V1-0002, type: data_integrity, given: 某个绑定到逻辑 Asset 的 SourceObject 被删除或来源永久不可达, when: 来源同步确认删除或失效, then: 仅更新该 SourceObject 或来源关系状态，不级联删除逻辑 Asset；Asset 元数据、其他来源、缓存、Rendition 和正式 Replica 继续保留，除非独立生命周期规则明确批准 Asset 删除}, {requirementId: REQ-V1-0002, type: data_integrity, given: 资产被合并、拆分或产生新版本, when: 读取历史和当前关系, then: 历史来源、版本和正式副本引用不产生悬空或静默覆盖}, {requirementId: REQ-V1-0002, type: production_gate, given: 资产模型准备进入生产, when: 执行移动、改名、版本、重复、合并、拆分和来源删除验收, then: 成功和失败场景均有可复现 Evidence 后才通过}]}, {id: 验收V1-0002, title: 权限隔离与受控按需访问, requirementIds: [REQ-V1-0003, REQ-V1-0008], scenarios: [{requirementId: REQ-V1-0003, type: success, given: 用户拥有资产权限且源支持 Range, when: 请求播放或预览, then: 授权后的 Range/缓存链路返回正确版本内容}, {requirementId: REQ-V1-0003, type: failure, given: 源站慢、断流或 Range/ETag 条件失效, when: 播放链路遇到故障, then: 返回可观察失败或受控降级且不返回错误版本}, {requirementId: REQ-V1-0003, type: permission, given: 请求者无资产权限, when: 请求搜索、缩略图、播放或缓存资源, then: 标题、摘要、缩略图和媒体字节均不泄漏}, {requirementId: REQ-V1-0008, type: success, given: 请求携带有效身份和权限上下文, when: 访问受控资源, then: IAM/ACL/签名授权按策略放行并产生审计证据}, {requirementId: REQ-V1-0008, type: failure, given: 签名过期、身份无效或策略不可判定, when: 访问受控资源, then: 默认拒绝且不得降级为匿名放行}, {requirementId: REQ-V1-0008, type: permission, given: 无权用户通过索引、缓存 URL 或资源 ID 探测, when: 访问受控元数据或内容, then: 结果为空或拒绝且权限泄漏为零}, {requirementId: REQ-V1-0008, type: data_integrity, given: 权限在索引、缓存或 AI 派生产物生成后变化, when: 再次查询、读取派生产物或播放, then: 最终权限复核使用当前 Asset ACL，旧索引、缓存或派生产物不能维持越权访问}, {requirementId: REQ-V1-0008, type: permission, given: 管理员已经通过密码登录但未进行本次高风险操作的 step-up 确认, when: 尝试发布、修改 ACL/Secret 或执行破坏性操作, then: 操作被拒绝并要求重新认证或二次确认，尝试写入审计}, {requirementId: REQ-V1-0008, type: production_gate, given: IAM/ACL/匿名/签名及管理员高风险路径进入发布候选, when: 执行 IDOR、ACL、签名 URL、OAuth、缓存隔离和 step-up 测试, then: 无权限泄漏且高风险操作无法只凭基础密码会话绕过二次确认时才允许发布}]}, {id: 验收V1-0003, title: Range、ATS 与缓存恢复, requirementIds: [REQ-V1-0003], scenarios: [{requirementId: REQ-V1-0003, type: recovery, given: ATS 源站连接中断或完整缓存未完成, when: 故障恢复或用户重试, then: 从安全边界重试并校验版本哈希且部分内容不标为完整}, {requirementId: REQ-V1-0003, type: failure, given: 源对象 ETag 或版本变化, when: 继续使用旧缓存或 If-Range, then: 旧内容不得与新版本拼接且失效过程可观察}]}, {id: 验收V1-0005, title: AV1/ABR 质量、重试与媒体产物追踪, requirementIds: [REQ-V1-0003], provenance: {type: PROGRAM_SUPPLEMENT, source: specs/coordination/program-plan.yaml}, scenarios: [{requirementId: REQ-V1-0003, type: success, given: 输入媒体、允许协议/路径和批准编码配置有效且资源配额允许, when: 执行 AV1/ABR 或临时兼容转码, then: 产物记录输入哈希、输出哈希、编码参数、工具版本和来源，并可关联质量验证结果}, {requirementId: REQ-V1-0003, type: failure, given: FFmpeg 输入协议、本地路径、开放代理目标或资源请求不在允许范围, when: 尝试启动媒体任务, then: 任务在启动前被拒绝，不能读取任意本地文件、访问未授权协议或形成开放代理}, {requirementId: REQ-V1-0003, type: failure, given: CPU、内存、执行时长、临时空间或输出大小达到已配置的硬限制, when: 媒体任务继续执行, then: 任务被受控终止或暂停并进入可观察失败状态，临时资源按策略清理；GZ-004 不虚构具体数值阈值}, {requirementId: REQ-V1-0003, type: recovery, given: 编码 Worker 中断或可重试错误发生, when: 按照运行时契约重试或恢复, then: 副作用幂等且不会把部分或错误产物标记为已验证成品}, {requirementId: REQ-V1-0003, type: production_gate, given: AV1/ABR 被声明为生产能力, when: 执行 POC-02、Golden Sample、质量、稳定性和 provenance 验收, then: 对应 POC 与 GZ-011 批准阈值通过前不得宣称生产达标}]}, {id: 验收V1-0006, title: 媒体兼容、AV1/ABR 与临时转码门禁, requirementIds: [REQ-V1-0003], scenarios: [{requirementId: REQ-V1-0003, type: production_gate, given: AV1、ABR 或 A380 临时 H.264 被声明为生产能力, when: 执行固定样本质量、重试、取消、清理与产物追踪测试, then: 对应 POC 和批准质量阈值完成后才可通过}]}, {id: 验收V1-0007, title: AI 固定样本、权限与事实边界, requirementIds: [REQ-V1-0004], scenarios: [{requirementId: REQ-V1-0004, type: success, given: 批准的媒体、图片、文档固定样本可用, when: 执行 AI 流水线, then: 产物模型、Prompt、参数、来源和处理链可追溯}, {requirementId: REQ-V1-0004, type: failure, given: 模型超时、GPU OOM、Provider 失败或输出不满足 Schema, when: 执行 AI 任务, then: 任务可重试或降级且失败输出不写成权威事实}, {requirementId: REQ-V1-0004, type: data_integrity, given: AI 输出字幕、翻译、摘要、标签、OCR、Embedding 或缩略图, when: 创建或读取派生产物, then: 派生产物继承源 Asset ACL，保留原始证据版本和人工干预记录，权限变化后按当前 ACL 复核}, {requirementId: REQ-V1-0004, type: data_integrity, given: 模型返回低置信度或不确定结果, when: 持久化并展示 AI 结果, then: confidence/uncertainty 被保留且结果明确标为低置信度，不能被展示为无条件权威事实}, {requirementId: REQ-V1-0004, type: data_integrity, given: 生成式缩略图和真实媒体关键帧同时存在, when: 保存、API 返回或 UI 展示缩略图, then: 生成式缩略图具有机器可读 generated 标记并在展示层可见区分，不能冒充真实关键帧}, {requirementId: REQ-V1-0004, type: permission, given: 商业 AI 或外部 Provider 调用准备发送资产内容或派生数据, when: 执行 Provider 调用前置检查, then: 只有调用者权限、当前 Asset ACL、数据策略均允许且硬预算或配额未超限才发送，否则默认拒绝并审计}, {requirementId: REQ-V1-0004, type: recovery, given: Worker 或 Provider 中断后恢复, when: 重放长任务, then: 保持幂等和产物版本一致}, {requirementId: REQ-V1-0004, type: production_gate, given: AI 能力准备发布, when: 按语言、质量、时长、场景运行 Golden Dataset，并验证权限、数据策略和预算门禁, then: 只有 GZ-011 阈值和 POC-09 结论满足且商业 Provider 控制可验证时通过}]}, {id: 验收V1-0008, title: 检索推荐相关性与权限门禁, requirementIds: [REQ-V1-0005], scenarios: [{requirementId: REQ-V1-0005, type: success, given: 索引已构建且用户有权限, when: 执行关键词、向量、混合检索或推荐, then: 调用者 ACL scope 在关键词或向量召回前限制候选集合，Fusion/Reranker 只处理授权候选，并在最终结果阶段再次 ACL 复核}, {requirementId: REQ-V1-0005, type: failure, given: OpenSearch、Milvus 或 Reranker 不可用, when: 执行搜索, then: 按批准路径降级或明确失败且不泄漏数据}, {requirementId: REQ-V1-0005, type: permission, given: 索引包含用户无权资产, when: 执行检索或推荐, then: 无权资产在召回前被权限范围过滤，最终结果也不包含受限资产及标题、摘要、缩略图}, {requirementId: REQ-V1-0005, type: recovery, given: 索引损坏或升级后重建, when: 恢复搜索服务, then: 索引可从权威资产数据重建并验证权限一致性}, {requirementId: REQ-V1-0005, type: production_gate, given: 相关性能力准备发布, when: 固定查询集测量 Recall/MRR/NDCG 并执行权限泄漏测试, then: GZ-011 阈值通过且召回前和结果后权限门禁均证明泄漏为零时通过}]}, {id: 验收V1-0004, title: 缓存、正式副本与生命周期完整性, requirementIds: [REQ-V1-0006], scenarios: [{requirementId: REQ-V1-0006, type: success, given: 对象满足 Promotion/Retention 且水位允许, when: 创建完整缓存或正式副本, then: 内容经过哈希完整性验证并记录副本状态位置}, {requirementId: REQ-V1-0006, type: failure, given: 空间不足、哈希不一致或存储断开, when: 执行缓存或副本任务, then: 不完整对象不得标为正式副本且失败可恢复}, {requirementId: REQ-V1-0006, type: data_integrity, given: 生命周期策略淘汰普通缓存, when: 执行缓存删除, then: 仅删除可重建缓存字节，不删除或修改逻辑 Asset 元数据、正式 Replica、Retention 状态及不可替代来源关系}, {requirementId: REQ-V1-0006, type: data_integrity, given: 生命周期策略迁移正式副本, when: 执行迁移, then: 正式副本在新位置完成完整性验证前旧有效副本和保留规则不被静默破坏}]}, {id: 验收V1-0011, title: 备份、恢复与关键数据回读, requirementIds: [REQ-V1-0006, REQ-V1-0010], scenarios: [{requirementId: REQ-V1-0006, type: recovery, given: 存在 PostgreSQL、Secrets、关键文件和 Bundle 备份, when: 执行实际恢复演练, then: 恢复数据可读取且与备份点一致}, {requirementId: REQ-V1-0006, type: production_gate, given: 备份恢复能力准备发布, when: 执行 POC-10 和批准恢复验收, then: 实际恢复、回读、限制和失败替代均有 Evidence 后通过}, {requirementId: REQ-V1-0010, type: success, given: 生产治理资产具有备份、审计和恢复信息, when: 执行受控恢复, then: 恢复操作可审计且关键数据配置可验证回读}, {requirementId: REQ-V1-0010, type: failure, given: 备份不可读、解密失败或恢复校验不一致, when: 执行恢复, then: 恢复被判失败并保留原始证据，不以文件存在或口头确认冒充成功}, {requirementId: REQ-V1-0010, type: recovery, given: 数据库、Secrets、关键文件或 Bundle 不可用, when: 执行灾难恢复, then: 按批准流程恢复并记录实际 RPO/RTO，未实测值不得宣称达标}]}, {id: 验收V1-0009, title: 规则模拟、冲突与发布, requirementIds: [REQ-V1-0007], scenarios: [{requirementId: REQ-V1-0007, type: success, given: 规则草稿通过 Schema 和权限验证, when: 执行模拟或受控运行, then: 决策结果具有规则版本、输入输出和审计}, {requirementId: REQ-V1-0007, type: failure, given: 规则冲突、表达式错误或脚本越权, when: 尝试发布或执行, then: 失败闭合并阻断发布}, {requirementId: REQ-V1-0007, type: data_integrity, given: 规则触发数据变更或任务创建, when: 提交结果, then: 幂等键、来源事件和执行版本可追溯}]}, {id: 验收V1-0010, title: Temporal 长任务暂停、恢复与故障语义, requirementIds: [REQ-V1-0007], scenarios: [{requirementId: REQ-V1-0007, type: success, given: 长任务处于允许暂停的运行状态, when: 请求 pause, then: 任务进入可观察的 paused 状态并停止继续产生新的业务副作用}, {requirementId: REQ-V1-0007, type: recovery, given: 任务处于 paused 且恢复条件满足, when: 请求 resume, then: 任务从安全检查点继续，保持幂等并产生可观察状态转换}, {requirementId: REQ-V1-0007, type: failure, given: 任务不支持暂停、状态不允许或 pause/resume 请求校验失败, when: 请求 pause 或 resume, then: 请求失败闭合，状态保持一致并返回可观察错误详情，不伪造暂停或恢复成功}, {requirementId: REQ-V1-0007, type: recovery, given: Worker 消失、Temporal 重启、网络中断或任务取消, when: 恢复或重放 Workflow, then: 按 Retry、Timeout、Heartbeat、Cancellation 契约恢复且副作用幂等}, {requirementId: REQ-V1-0007, type: production_gate, given: 长任务进入生产, when: 执行重试、pause、resume、取消、Worker 离线和故障注入, then: 状态转换可观察、失败路径可恢复且不阻塞核心浏览播放时通过}]}, {id: 验收V1-0012, title: 配置、部署、供应链与生产发布, requirementIds: [REQ-V1-0009, REQ-V1-0010], scenarios: [{requirementId: REQ-V1-0009, type: success, given: 配置、规则、模型或部署变更形成草稿, when: 执行验证、模拟、审批、灰度和发布, then: 每一步有版本、审批、审计和回滚点}, {requirementId: REQ-V1-0009, type: failure, given: 验证、审批、灰度或健康检查失败, when: 尝试继续发布, then: 发布被阻断或回滚到已知良好版本}, {requirementId: REQ-V1-0009, type: data_integrity, given: 升级或配置变更影响数据 Schema, when: 执行迁移或部署, then: 迁移和回滚可验证且不允许无正式 Migration 的数据行为}, {requirementId: REQ-V1-0009, type: recovery, given: 新配置或部署造成服务异常, when: 执行回滚, then: 恢复到批准版本并保留失败 Evidence}, {requirementId: REQ-V1-0009, type: production_gate, given: 候选版本申请正式发布, when: 执行签名、SBOM、许可证、Bundle 和回滚验证, then: 全部批准门禁通过后才能进入 GZ-020}, {requirementId: REQ-V1-0010, type: failure, given: 关键监控、审计、恢复或供应链 Evidence 缺失, when: 申请生产发布, then: 发布失败闭合且口头确认不能替代 Evidence}, {requirementId: REQ-V1-0010, type: permission, given: Agent 或操作员尝试绕过审批权限直接修改生产, when: 执行变更, then: 操作被阻断或要求人工批准且尝试进入审计}, {requirementId: REQ-V1-0010, type: data_integrity, given: 配置、规则、模型、任务、部署或数据变化发生, when: 查询审计链, then: 能关联主体、版本、输入、结果和回滚恢复证据}, {requirementId: REQ-V1-0010, type: production_gate, given: V1 申请正式发布, when: 检查范围能力、漏洞、恢复、播放、AI、文档、制品和人工签署, then: V1 无对外 Beta 且所有门禁及 OPS-001 关闭后才允许发布}]}]}
