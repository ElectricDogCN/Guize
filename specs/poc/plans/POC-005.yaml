schemaVersion: 1
pocId: POC-05
taskId: POC-005
title: 700TB 文件数、目录规模、变更率与扫描成本
status: planned
resultStatus: not_started
riskLevel: medium
wave: W2
dependsOn:
- GZ-010
requirementIds:
- REQ-V1-0001
- REQ-V1-0002
moduleIds:
- MOD-SOURCE
- MOD-ASSET
- MOD-CONNECTOR
evidencePath: evidence/POC-005
resourceIds:
- RES-METADATA-SCALE
sampleIds:
- SAMPLE-METADATA-SYNTHETIC
environment:
  captureBeforeExecution:
  - host_os
  - kernel
  - cpu
  - memory
  - gpu
  - driver
  - runtime
  - tool_versions
  - network_profile
  - storage_profile
  capturedValues: {}
protocol:
  commands: []
  rawOutputRefs: []
  measurements:
  - id: file_count
    metric: Representative source file count
    unit: files
    method: Count immutable metadata records in the approved source/synthetic hierarchy and retain the enumeration manifest reference.
    thresholdSource: NFR_MEASUREMENT_REQUIRED
    actual: null
  - id: directory_count
    metric: Representative directory count
    unit: directories
    method: Count directory nodes from the same immutable hierarchy used for file-count and scan tests.
    thresholdSource: NFR_MEASUREMENT_REQUIRED
    actual: null
  - id: directory_depth_max
    metric: Maximum observed directory depth
    unit: levels
    method: Compute maximum path depth from the approved hierarchy manifest.
    thresholdSource: NFR_MEASUREMENT_REQUIRED
    actual: null
  - id: average_file_size
    metric: Average file size for the representative source population
    unit: bytes
    method: Calculate from approved metadata size fields or immutable synthetic fixture metadata without transferring production payload.
    thresholdSource: NFR_MEASUREMENT_REQUIRED
    actual: null
  - id: source_api_quota
    metric: Source API/listing quota or rate-limit characteristic
    unit: requests_per_minute
    method: Record the documented or sandbox-observed listing/request quota for the tested source adapter profile, including reset/backoff semantics.
    thresholdSource: NFR_MEASUREMENT_REQUIRED
    actual: null
  - id: source_change_frequency
    metric: Observed source metadata change frequency
    unit: changes_per_hour
    method: Apply and observe the approved deterministic create/update/delete change stream, recording event count and observation window from
      the immutable fixture manifest.
    thresholdSource: NFR-V1-CAP-003
    actual: null
  - id: enumeration_rate
    metric: Metadata enumeration rate
    unit: objects/s
    method: Scan synthetic/approved representative hierarchy and record rate.
    thresholdSource: POC_EXIT_CRITERIA
    actual: null
  - id: incremental_scan_cost
    metric: Incremental scan cost
    unit: s
    method: Replay controlled change sets and measure incremental traversal.
    thresholdSource: POC_EXIT_CRITERIA
    actual: null
  - id: memory_peak
    metric: Scanner peak memory
    unit: MiB
    method: Capture process peak memory during scale sweeps.
    thresholdSource: POC_EXIT_CRITERIA
    actual: null
  - id: filename_characteristics_profiled
    metric: Filename and path characteristics are profiled
    unit: boolean
    method: Measure name length, character-set, extension and path-depth distributions from the approved immutable metadata fixture.
    thresholdSource: APPROVED_POC_SCOPE
    actual: null
  - id: provider_characteristics_profiled
    metric: Provider/source characteristics are recorded
    unit: boolean
    method: Record provider listing, pagination, consistency, timestamp and object-identity semantics for the tested source profile.
    thresholdSource: APPROVED_POC_SCOPE
    actual: null
  - id: range_support_profiled
    metric: Source Range support is measured
    unit: boolean
    method: Exercise supported and unsupported Range requests and record status/header/body behavior for the tested provider profile.
    thresholdSource: APPROVED_POC_SCOPE
    actual: null
  - id: etag_behavior_profiled
    metric: Source ETag or equivalent version semantics are measured
    unit: boolean
    method: Record validator stability for unchanged objects and change behavior after an approved source update.
    thresholdSource: APPROVED_POC_SCOPE
    actual: null
  exitCriteria:
  - File count, directory count/depth, average file size and source API quota/rate-limit facts are captured for the immutable representative hierarchy/source
    profile.
  - Source change frequency is measured from a deterministic create/update/delete observation window rather than asserted in prose.
  - Scale model covers file count, directory fanout/depth and change rate with raw manifest/provenance references.
  - Enumeration rate, incremental scan cost and memory peak are measured without assuming 700TB byte transfer.
  - Filename/path and provider characteristics, Range support and ETag/version semantics are captured for the tested source profile.
  failureFallbacks:
  - If direct full enumeration is infeasible or source quotas dominate cost, evaluate incremental journals/change tokens, adaptive backoff or
    staged indexing.
  rollbackCleanup:
  - Stop test workloads and remove temporary test artifacts.
  - Restore sandbox configuration to the pre-POC snapshot or document irrecoverable drift.
  - Do not delete governance/Evidence history.
security:
  dataClassification: non-sensitive
  sampleApprovalRequired: true
  credentialsStoredInRepository: false
  privacyReviewRequired: false
  licenseReviewRequired: false
decision:
  status: not_evaluated
  rationale: null
  resultRef: null
review:
  reviewer: null
  approvedAt: null
  approval: null
